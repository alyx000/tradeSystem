---
description: 重大功能/架构变更须先设计分层测试方案
globs:
alwaysApply: true
---

# 分层测试设计规则

## 触发条件

当改动涉及以下任一情形时，须在开发前先输出分层测试方案：

- 新模块引入（新的包 / 目录 / 独立功能单元）
- 架构层级变更（新增存储层 / 引入新协议 / 重构核心数据流）
- 新数据存储或通信协议（数据库 / 缓存 / 消息队列 / API 协议）
- 预估影响 3+ 个文件

## 规则内容

1. **开发前输出分层测试方案**：明确测试分哪几层（如 数据层 / 业务逻辑 / API / UI）、每层覆盖哪些场景、层间依赖关系
2. **测试金字塔原则**：底层用例多且快（纯函数 / DB），顶层用例少且聚焦（E2E / UI），禁止倒金字塔（全靠 E2E 测试兜底）
3. **每层须有明确的完成标准**：不是「写了测试」而是「X 个场景全部通过 + 不引入已有回归」
4. **隔离原则**：每层 mock 掉下层依赖（DB 用 tmp_path / 内存库、API 用 TestClient、外网/密钥用 mock），禁止测试依赖真实环境
5. **执行顺序**：自底向上——底层通过后才推进上层，而非先写 UI 测试再补数据层

## 与 dev-workflow 的关系

- `dev-workflow.mdc` 管每次提交的纪律（必须有测试、必须跑通）
- `test-design.mdc` 管项目/模块级的测试结构设计
- 两者互补，不冲突
