---
description: 技术方案、执行计划、业务逻辑解析统一使用结构化文档与 Mermaid 标准
alwaysApply: true
---

# 技术方案输出格式规则

## 适用范围

当 AI 向用户输出以下内容时，默认触发本规则：

- 技术方案
- 架构设计
- 执行计划
- 业务逻辑解析
- API / 数据模型设计
- 状态流转与系统交互说明

纯闲聊、简短问答、单点代码解释、一次性排查结论，不强制套用完整模板。

## 结构化输出要求

1. 使用 `##` 到 `####` 组织内容，避免大段纯文本。
2. 关键标识符必须使用反引号，包括：
   - 变量名、函数名、类名
   - 文件名、目录名、命令名、表名、字段名、接口路径
3. 重要结论、约束、风险使用 **加粗** 或引用块。
4. 动作清单、实施步骤、待办事项必须使用有序列表或 Checklist。

## 默认章节骨架

若用户未指定格式，技术方案默认按以下章节展开：

1. `方案结论`
2. `背景与目标`
3. `范围与非目标`
4. `现状与约束`
5. `方案设计`
6. `数据模型`
7. `API 设计`
8. `实施计划`
9. `测试与验证`
10. `风险与回滚`
11. `待确认问题`

如果不涉及某一章节，可明确写“本次不适用”，不要静默省略。

## 表格强制要求

以下内容禁止用散文描述，必须使用 Markdown 表格：

### 数据库表 / 数据字典

必须包含列：

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|

### API 契约

至少包含：

| 项 | 内容 |
|----|------|
| Method | `GET/POST/PUT/...` |
| URI | `/api/...` |
| 说明 | 接口职责 |

并补充格式化的 JSON 请求/响应示例。

### 方案对比 / 影响面 / 风险

涉及多方案选择、跨层影响、风险评估时，优先使用表格表达。

## Mermaid 强制要求

当内容涉及系统交互、模块依赖、架构演进、状态流转时，必须输出 Mermaid 图。

### 架构图 / 模块依赖图

- 使用 `flowchart TB` 或 `flowchart LR`
- 必须优先使用 `subgraph` 表达层级
- 数据库节点优先使用 `[(Database)]`
- 判断节点优先使用 `{Decision}`

### 时序图

- 使用 `sequenceDiagram`
- 必须使用 `autonumber`
- 阶段分组优先使用 `rect rgb(...)`
- 关键业务规则使用 `Note right of ...`

### 状态图

- 使用 `stateDiagram-v2`
- 状态切换边上标注触发动作

### 禁止项

- 禁止使用 PlantUML
- 禁止以“后续可补图”替代当前应提供的图

## 结论与验证要求

1. 方案开头优先给出 `方案结论` 或 `推荐方案`。
2. 实施计划必须包含：
   - 影响层次
   - 实施步骤
   - 验证命令
   - 完成标准
3. 涉及 schema、CLI、API、状态机、默认值变更时，必须单列：
   - 兼容性
   - 迁移方案
   - 回滚策略
4. 结尾必须显式列出 `待确认问题`，避免隐含假设直接进入实现。

## 模板入口

优先复用以下模板作为起点：

- `docs/templates/technical-design.md`
- `docs/templates/execution-plan.md`
- `docs/templates/api-contract.md`
