---
description: 开发工作流规范：每次开发必须包含验证测试，通过后才算完成
alwaysApply: true
---

# 开发工作流规范

每次开发任务必须遵循以下流程，**验证测试通过后才算开发完成**。

## 必须执行的三阶段

### 1. 开发前：设计验证方案
在开始写代码之前，明确说明将用什么方式验证本次改动：
- 运行哪个命令
- 期望看到什么输出
- 哪些 Warning/Error 是不可接受的

### 2. 开发中：按计划实现
- 完成所有代码改动
- 运行 `ReadLints` 检查 lint 错误，有则修复

### 3. 开发后：执行验证并报告结果
- 运行之前设计的验证命令
- 逐项对照期望输出汇报：✅ 通过 / ❌ 失败
- 失败项必须修复后重新验证，直到全部通过
- 最终向用户呈现验证报告

## 此项目的验证命令参考

| 改动范围 | 验证命令 |
|----------|----------|
| 盘前流程 | `cd scripts && python3 main.py pre --date <近期交易日>` |
| 盘后流程 | `cd scripts && python3 main.py post --date <近期交易日>` |
| provider 新方法 | 在 `scripts/` 下写临时 Python 脚本直接调用并打印结果 |
| 仅报告生成器 | 传入 mock dict 调用 `ReportGenerator().generate_pre_market(...)` |

## 验证报告格式（每次必须输出）

```
验证结果：
- [✅/❌] 功能A：<实际输出摘要>
- [✅/❌] 功能B：<实际输出摘要>
- 遗留问题：<已知限制，如数据源不支持某字段>
```
