interface:
  display_name: "仓库维护"
  short_description: "只读诊断、代码审查、跨入口语义对齐、文档同步与仓库验证"
  default_prompt: "Use $repo-maintenance-workflows to inspect tradeSystem read-only first, then apply only authorized fixes and run proportional validation."
